**Unreleased**

* Reject unsafe iptables chain names before constructing firewall commands.
* Prevent shell-command injection through logoff-user and block-IP action parameters.
* Preserve supported hostname and custom-chain inputs while safely constructing firewall commands.
