**Unreleased**

* Execute program now reads script files only from vault attachments in the current container.
* Block IP rejects invalid directions instead of silently creating an outbound firewall rule.
